template(name='noteHandle')
  .handle.noteNumber {{count}}
  .handle.dot.mdl-button.mdl-button--icon.hasChildren(title="{{hoverInfo}}")
    i.material-icons.zoom(id="drag_{{_id}}") fiber_manual_record